---
summary: "Add the package.json mcpName field so the server can publish to the MCP Registry."
breaking: false
---

# 0.1.1 — 2026-06-14

## Fixed

- **`mcpName`** — added `"io.github.cyanheads/finnhub-mcp-server"` to `package.json`. The field is required for MCP Registry validation; without it the 0.1.0 registry publish could not complete.
